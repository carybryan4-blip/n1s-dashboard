# Port to run the server on (Render sets this automatically)
PORT=3001

# Secret key for the sync endpoint - make this long and random
# Must match the apiKey in Unity's DashboardSync component
SYNC_API_KEY=change-this-to-something-random-and-long
